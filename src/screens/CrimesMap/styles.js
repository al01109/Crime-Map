import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
  },
  flatList: {
    position: 'absolute',
    bottom: 10,
  },
});

export default styles;
