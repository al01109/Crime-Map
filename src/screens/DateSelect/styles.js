import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    heading: {
        textAlign: 'center',
        fontSize: 24,
        marginTop: 25,
    },
});

export default styles;